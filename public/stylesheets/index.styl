* {
  margin: 0;
  padding: 0;
}

body {
  font: 14px "Lucida Grande", Helvetica, Arial, sans-serif;
  color: #FCFCFC;
}

.grid {
  overflow: hidden;
  margin-left: -20px;
  padding-left: 0;
}
.grid__item {
  box-sizing: border-box;
  display: inline-block;
  width: 100%;
  padding-left: 20px;
  margin-left: 0;
  list-style: none;
}
.one-half {
  width: 50%;
}
.one-quarter {
  width: 25%;
}


.timesheet-graph {
  width: 752px;
  margin: 0 auto;
  position: absolute;
  top: 50%;
  left: 50%;
  -webkit-transform: translate(-50%, -50%);
  transform: translate(-50%, -50%);
}

.timesheet-graph__header {
  text-align: center;
}

.timesheet-graph__title {
  text-transform: uppercase;
  margin-bottom: 10px;
  font-weight: normal;
  font-size: 36px;
}

.timesheet-graph__meta {
  text-transform: uppercase;
  margin-bottom: 20px;
  font-size: 24px;
}


.employee-list__header {
  position: relative;
  height: 4px;
  margin: 25px 0 50px 0;
  border-color: #fff;
  border-style: solid;
  border-width: 1px 1px 0 1px;
}

.employee-list__title {
  position: absolute;
  top: -16px;
  left: 50%;
  padding: 0 10px;
  -webkit-transform: translate(-50%, 0);
  background-color: #020202;
  font-weight: normal;
  color: #fff;
  text-transform: uppercase;
  font-size: 30px;
}

.employee-list__content {
  width: 365px;
  margin: 0 auto;
}


.employee {

}

.employee__graph__label {
  text-transform: uppercase;
  font-size: 24px;
  text-align: center;
  margin-bottom: 12px;
}

.employee__graph__output {
  background-image: url(../images/employee__graph__output-bg.png);
  -webkit-transform: rotate(0.5turn) translate3d(0, 0, 0);
  margin-bottom: 2px;
}

.employee__avatar {
  border: 2px solid #fff;
  position: relative;
  width: 72px;
  height: 72px;
}

.employee__initials {
  position: absolute;
  top: 0;
  left: 0;
  width: 72px;
  height: 72px;
  text-align: center;
  line-height: 72px;
  font-size: 2em;
}

.employee__image {
  position: absolute;
  top: 0;
  left: 0;
}